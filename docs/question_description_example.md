# 题目描述示例（Markdown格式）

## 示例1：简单的编程题

**题目：两数之和**

给定一个整数数组 `nums` 和一个目标值 `target`，请你在该数组中找出和为目标值的那两个整数，并返回它们的数组下标。

你可以假设每种输入只会对应一个答案。但是，数组中同一个元素不能使用两遍。

**示例 1：**

```
输入：nums = [2, 7, 11, 15], target = 9
输出：[0, 1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
```

**示例 2：**

```
输入：nums = [3, 2, 4], target = 6
输出：[1, 2]
```

**示例 3：**

```
输入：nums = [3, 3], target = 6
输出：[0, 1]
```

**提示：**

- `2 <= nums.length <= 10^4`
- `-10^9 <= nums[i] <= 10^9`
- `-10^9 <= target <= 10^9`
- 只会存在一个有效答案

---

## 示例2：函数题

**题目：计算斐波那契数列**

编写一个函数 `fibonacci(n)`，计算斐波那契数列的第 `n` 项。

斐波那契数列的定义如下：

- `F(0) = 0`
- `F(1) = 1`
- `F(n) = F(n - 1) + F(n - 2)`，其中 `n > 1`

**示例：**

```python
输入：n = 4
输出：3
解释：斐波那契数列为：0, 1, 1, 2, 3, 5, 8, 13...
     第4项是3
```

**函数签名：**

```python
def fibonacci(n: int) -> int:
    pass
```

**约束条件：**

- `0 <= n <= 30`

---

## 示例3：带图片和表格的题目

**题目：学生成绩统计**

编写一个函数 `calculate_average(scores)`，计算学生成绩的平均分。

**输入格式：**

`scores` 是一个字典，键为学生姓名（字符串），值为成绩列表（整数列表）。

**输出格式：**

返回一个字典，键为学生姓名，值为该学生的平均分（保留两位小数）。

**示例：**

```python
输入：
scores = {
    "张三": [85, 90, 78],
    "李四": [92, 88, 95],
    "王五": [76, 82, 80]
}

输出：
{
    "张三": 84.33,
    "李四": 91.67,
    "王五": 79.33
}
```

**说明：**

- 每个学生的成绩可能有多个科目
- 平均分 = 所有成绩之和 / 成绩数量
- 结果保留两位小数

---

## Markdown常用语法参考

### 标题
```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 强调
```markdown
**粗体**
*斜体*
`代码`
```

### 列表
```markdown
- 无序列表项1
- 无序列表项2

1. 有序列表项1
2. 有序列表项2
```

### 代码块
````markdown
```python
def example():
    return "Hello"
```
````

### 表格
```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 数据1 | 数据2 | 数据3 |
```

### 引用
```markdown
> 这是一个引用块
```

### 分割线
```markdown
---
```




